# kotoba-lang/shacl

Small EDN shape validator inspired by SHACL.

This is a pragmatic validator for kotoba maps and RDF-like node maps. Full SHACL
SPARQL/RDF graph semantics are left for a later processor layer.

## Test

```bash
clojure -M:test
```
